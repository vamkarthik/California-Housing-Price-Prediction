import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import Navbar from "@/components/Navbar";
import "./globals.css";


const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <head>
        <title>California Housing Price Prediction</title>
      </head>
      <body className="h-screen flex flex-col">
          <div className="flex items-center bg-gray-100 py-0 px-0">
            <img src="/phoenix.png" alt="Logo" className="h-20 w-35 mr-65" />

            <h1 className="text-3xl text-blue-800 font-bold text-center">California Housing Price Prediction</h1>
        </div>

        <div className="flex flex-1">
          <nav className="w-35 bg-blue-800 text-white p-4 flex flex-col space-y-4">
            <a href="/" className="text-lg font-semibold hover:underline">Home</a>
            <a href="/contact" className="text-lg font-semibold hover:underline">Contact Us</a>
          </nav>

          <main className="flex-1 p-6 bg-gray-100">{children}</main>
        </div>
      </body>
    </html>
  );
}

